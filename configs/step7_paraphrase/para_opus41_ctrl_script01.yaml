# STEP7 WITHIN-ROUTE CONTROL — Opus 4.1, blackmail, A3 script_01, n=120, via the
# SAME openrouter route as the paraphrase cells. Isolates script-specificity from
# a route/snapshot difference: the original 62.5% used native anthropic/, but the
# paraphrases used openrouter/anthropic/claude-opus-4.1. Without this control the
# paraphrase comparison confounds script with route.
experiment_id: step7_para_opus41_ctrl_script01
description: STEP7 within-route control — Opus 4.1 blackmail, A3 script_01 via openrouter, n=120.
seed: 12345
model: openrouter/anthropic/claude-opus-4.1
grader_model: null
temperature: 1.0
max_connections: 20
reasoning_effort: null
log_base: logs/step7_para/opus41_ctrl_script01
results_root: results
common:
  scenario: blackmail
  goal_type: latent
  urgency_type: restriction
  extra_system_instructions: dont
  cf: false
  manipulation_check: true
  score_eval_awareness: false
levels:
  A3:
    scripts: [script_01]
    n_per_script: 120
cost:
  avg_tokens_per_rollout: 2500
  usd_per_1k_tokens: 0.03
  max_usd: 20.0
