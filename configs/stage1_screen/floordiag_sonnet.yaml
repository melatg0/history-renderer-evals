# Claude floor-diagnostic — Sonnet 4.6, prime DROPPED (no `dont`).
# Q: is Sonnet's 0/0 floor refusal-driven (the `dont` safety prime suppressing an
# up-effect) or true zero-propensity? Drop the prime, keep everything else = pilot.
# A_raw+A3 n=40. If A_raw lifts off 0 AND A3>A_raw -> a SECOND up-model may be
# hiding under the floor (strengthens the "up family" claim, currently Opus-4.1-only).
# If still ~0 -> accept the up-direction is Opus-4.1-only and frame accordingly.
# NOTE: extra_system_instructions intentionally OMITTED -> task default None -> no prime.

experiment_id: stage1_floordiag_sonnet_noprime
description: Floor-diagnostic — Sonnet 4.6 with the `dont` prime removed. A_raw vs A3, blackmail, latent.

seed: 12345
model: anthropic/claude-sonnet-4-6
grader_model: null
temperature: 1.0
max_connections: 20
reasoning_effort: none         # same as pilot (thinking disabled)

log_base: logs/stage1_screen/floordiag_sonnet
results_root: results

common:
  scenario: blackmail
  goal_type: latent
  urgency_type: restriction
  # extra_system_instructions: OMITTED (was `dont`) -> no safety prime. This is the diagnostic lever.
  cf: false
  manipulation_check: true

levels:
  A_raw:
    scripts: []
    n: 40
  A3:
    scripts: []
    n: 40

cost:
  avg_tokens_per_rollout: 2500
  usd_per_1k_tokens: 0.009
  max_usd: 10.0
